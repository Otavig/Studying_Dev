---

Dia 1

Bom pelo que vi existe um site para acessar em primeira mão que é o 

- https://www.electronjs.org/

Ele roda node js em um chromium embaixo dos panos e ele é praticamente o software rodando no binario "By embedding Chromium and Node.js into its binary, Electron allows you to maintain one JavaScript codebase and create cross-platform apps that work on Windows, macOS, and Linux — no native development experience required."

Pelo que vi também eles tem um própria IDE de desenvolvimento que se chama Electron Fiddle. Para sua inicialização é bem fácil, vocẽ necessita dar NPM INIT como qualquer outro projeto node e em seguinda você precisa installar o electron com NPM INSTALL ELECTRON --SAVE-DEV e esse --SAVE-DEV é para declara que isso é só para desenvolvimento e não para produção

E o legal disso é que vou misturar com REACT + TYPESCRIPT, vamos ver como isso vai ficar

-----

Dia 2

Seguinte, eu andei lendo os documentos e descobri que ele é multi-process que significa que ele faz o alto gerenciamento e tudo mais. Bom como ele trabalha na parte binaria eu decidi dá uma estudada de threads de processador, descobri que quem decide o que é alta prioridade é o próprio sistema opericional e descobri umas outras coisas que depois volto aqui para desenvolver a ideia, mas vou me aprofundar mais antes de qualquer coisa! ```-VOLTAR AQUI-```.

Bom descobri que ele tem uma classe chamada BrowserWindow que vem da biblioteca ELECTRON, bom chamei ele como quem não quer nada e declarei uma constante nomeada de 'win' (window) para instanciar essa janela, onde os paramêtros são a largura x altura, bom eu coloquei a largura como 800, mas me perdi um pouco pelo tdah, e pensei poxa, como eu descubro a altura ideal por base da largura e no decorrer eu fui atrás, e bom descobri que existe uma formula mas que necessita de uma pre-ideia... Primeiro escolhemos a proporção 16:9, 4:3, 16:10 e etc.. Bom nossa proporção base é a 16:9, então se seria H = 800/16 = 50 * 9 = 450px. Então coloco no objeto largura e altura. Mas lembrando-se tem que se fazer isso dentro de uma função por exemplo "CreateWindow()" e em seguida vc tem que colocar o win.loadFile("index.html").


----
